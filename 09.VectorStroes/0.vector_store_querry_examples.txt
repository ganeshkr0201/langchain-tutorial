# Query for Extracting Data from the database

# search documents
result = vector_store.similarity_search(
    query='Who among these are a bowler?',
    k=2
)
print(result)


# search with similarity score
result_with_similarity_score = vector_store.similarity_search_with_score(
    query='Who among these are a bowler?',
    k=2
)
print(result_with_similarity_score + '\n\n\n')


# meta-data filtering
result_with_filter = vector_store.similarity_search_with_score(
    query="",
    filter={"team": "Chennai Super Kings"}
)
print(result_with_filter + '\n\n\n')


# update documents
updated_doc1 = Document(
    page_content="Virat Kohli, the former captain of Royal Challengers Bangalore (RCB), is renowned for his aggressive leadership and consistent batting performances. He holds the record for the most runs in IPL history, including multiple centuries in a single season. Despite RCB not winning an IPL title under his captaincy, Kohli's passion and fitness set a benchmark for the league. His ability to chase targets and anchor innings has made him one of the most dependable players in T20 cricket.",
    metadata={"team": "Royal Challengers Bangalore"},
)
vector_store.update_document(document_id="9b7408ae-0a80-4d1f-8915-3ca9d04d7275", document=updated_doc1)


# delete document
vector_store.delete(ids=['09a39dc6-3ba6-4ea7-927e-fdda591da5e4'])


# view documents
documents = vector_store.get(include=['embeddings','documents', 'metadatas'])
print(documents + '\n\n\n')